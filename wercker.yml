box: wercker/default
build:
  steps:
    - arjen/hugo-build：
        version: "0.17"
        theme: material-lite
        force_install: true
        flags: --disableSitemap=true
deploy:
  steps:
    - leipert/git-push:
        gh_oauth: $GITHUB_TOKEN
        basedir: public
        clean_removed_files: true
        branch: gh-pages
        gh_pages: true
