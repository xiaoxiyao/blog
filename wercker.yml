box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.17"
        theme: material-lite
        disable_pygments: true
        flags: --disableSitemap=true
deploy:
  steps:
    - osteele/gh-pages:
        token: $GIT_TOKEN
        basedir: public
        branch: gh-pages
